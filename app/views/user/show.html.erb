<% if flash[:notice] || flash[:alert]  %>
	<p><%= flash[:notice] %></p>
	<p><%= flash[:alert] %></p>	
<% end %>

<h1><%= @user.username %></h1>
<h3><%= @user.email %></h3>

<%= link_to "Create Tweet", new_user_tweet_path(@user) %>

<% if @user.tweets.any? %>
	<h3>Your Tweets</h3>

	<% @user.tweets.each do |tweet| %>
	<li><%= tweet.created_at.strftime("%m/%d/%Y") %></li>
	<li><%= tweet.body %></li>
	<br>
	<% end %>
<% else %>
	<h3>You don't have any Tweets</h3>
<% end %>


